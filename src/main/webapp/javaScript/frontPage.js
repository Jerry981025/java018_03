function doFirst() {
    loginBtn = document.getElementById('login')
    loginBtn.addEventListener('click', function () {

    })

    let body = {
        mEmail: mEmail,
        mPassword: `${mPassword}`,
    }
}